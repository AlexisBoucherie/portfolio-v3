<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_home') }}">Alexis Boucherie</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="portfolio-btn-container">
            <a href="{{ path('app_home') }}">
                <button type="button" class="btn btn-outline-primary m-1">Accueil</button></a>
            <a href="{{ path('app_about') }}">
                <button type="button" class="btn btn-outline-primary m-1">A propos</button></a>
            <a href="{{ path('app_projects') }}">
                <button type="button" class="btn btn-outline-primary m-1">Projets</button></a>
            <a href="{{ path('app_contacts') }}">
                <button type="button" class="btn btn-outline-primary m-1">Contacts</button></a>
        </div>
    </div>
</nav>